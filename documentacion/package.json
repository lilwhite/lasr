{
  "name": "lasr-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync:portal": "node scripts/sync-portal-assets.mjs",
    "predev": "npm run sync:portal",
    "dev": "astro dev",
    "prebuild": "npm run sync:portal",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check && npm run build && npm run check:links && npm run check:gate",
    "check:links": "python3 scripts/check_links.py",
    "check:gate": "python3 scripts/check_gate.py"
  },
  "dependencies": {
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3"
  }
}
